# Copy this file to `.env` and paste your Notion integration token.
# Alternatively, leave .env empty and enter the token in the app's setup screen
# (it will be written back into .env for you).
NOTION_TOKEN=
